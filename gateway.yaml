apiVersion: networking.k8s.io/v1
kind: Gateway
metadata:
  name: harsh-gateway
  namespace: harsh-ns
spec:
  gatewayClassName: kgateway
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: same
      